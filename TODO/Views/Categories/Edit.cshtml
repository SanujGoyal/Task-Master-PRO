@model TODO.Models.Categories

@{
    ViewData["Title"] = "Edit Category";
}

<h1 style="text-align: center; margin: 20px 20px;">Update Category!</h1>


<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-4">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div> 

                <input type="hidden" asp-for="CategoriesId" />

                <div class="form-group mb-3">
                    <label asp-for="CategoriesName" class="control-label">Category Name</label>
                    <input asp-for="CategoriesName" class="form-control form-control-sm mt-2" />
                    <span asp-validation-for="CategoriesName" class="text-danger"></span>
                </div>

                <div class="form-group text-center">
                    <input type="submit" value="Update Category!" class="btn btn-primary" />
                </div>
            </form>
        </div>
    </div>
</div>

<div style="text-align: center; margin-top: 20px;">
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
